# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Git Workflow

Always create a feature branch for changes and open a PR to merge into main. Never commit or merge directly to main.

## Project

This is a nascent repository for an LLM systems engineer roadmap. As of the initial commit, it contains only a README. The build system, language, and structure have not been established yet.

Once the project takes shape, update this file with:
- Build, lint, and test commands (including how to run a single test)
- High-level architecture decisions (e.g., monorepo layout, framework choices, data flow)
- Any non-obvious conventions or constraints
